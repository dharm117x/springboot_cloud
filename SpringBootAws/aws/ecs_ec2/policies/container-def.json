[
  {
    "name": "springboot-container",
    "image": "${image_url}",
    "essential": true,
    "environment": [
      { "name": "SPRING_PROFILES_ACTIVE", "value": "test" },
      { "name": "AWS_S3_BUCKET", "value": "${s3_bucket}" },
      { "name": "AWS_SQS_QUEUE", "value": "${sqs_queue}" },
      { "name": "AWS_SNS_TOPIC", "value": "${sns_topic}" },
      { "name": "SPRING_DATASOURCE_URL", "value": "jdbc:mysql://${rds_endpoint}:3306/${db_name}" },
	  { "name": "SPRING_DATASOURCE_USERNAME",	"value": "${db_user}"},
	  { "name": "SPRING_DATASOURCE_PASSWORD",	"value": "${db_password}"}
    ],
    "portMappings": [
      { "containerPort": 9001}
    ],
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "${log_group_name}",
        "awslogs-region": "${region}",
        "awslogs-stream-prefix": "ecs"
      }
    }
  }
]